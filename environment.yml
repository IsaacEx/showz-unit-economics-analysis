name: showz_env
channels:
  - conda-forge
  - defaults
  
dependencies:
  # Runtime
  - python=3.12
  - numpy>=2.0
  - pandas>=3.0
  - pyarrow>=14.0

  # Visualización
  - matplotlib>=3.9
  - seaborn>=0.13

  # Jupyter (notebooks y kernel)
  - jupyter>=1.0
  - ipykernel>=6.0
